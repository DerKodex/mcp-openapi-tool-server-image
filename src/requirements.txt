# Web framework & server
fastapi==0.114.2
uvicorn[standard]==0.30.6

# HTTP client
httpx==0.27.2

# Data validation (FastAPI pulls Pydantic v2, but make it explicit)
pydantic>=2.8,<3

# Model Context Protocol (stdio transport used by your bridge)
mcp[stdio]>=0.1.0

# Cache backend
redis>=5,<6

# YAML parsing for the manifest file
PyYAML>=6.0.1,<7

# Yugabyte/Postgres client + async pool (versions must match)
psycopg[binary]>=3.2,<3.3
psycopg_pool>=3.2,<3.3
